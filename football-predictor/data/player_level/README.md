# Player-level datasets

Downloaded and derived player-level datasets in this directory are ignored by
Git. They must be reproducible through the corresponding import scripts and
must retain source, license, observation-time, and mapping metadata.

Version field contracts and empty import templates under `data/templates`, not
the fetched datasets themselves.
